<UserControl x:Class="NodeLabFarm.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Application Logs" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="White"/>

        <ui:Card Grid.Row="1" Padding="0" Background="#0C0C0C" BorderThickness="1" BorderBrush="#2d2f39">
            <TextBox Text="{Binding CommandOutput, Mode=OneWay}" 
                     IsReadOnly="True" 
                     Background="Transparent" 
                     Foreground="#1DB954" 
                     BorderThickness="0" 
                     FontFamily="Consolas, Lucida Console" 
                     FontSize="13" 
                     Padding="16"
                     TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto" 
                     HorizontalScrollBarVisibility="Disabled"
                     AcceptsReturn="True"/>
        </ui:Card>
    </Grid>
</UserControl>
