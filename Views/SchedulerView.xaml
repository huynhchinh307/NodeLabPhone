<UserControl x:Class="NodeLabFarm.Views.SchedulerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:NodeLabFarm.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    
    <UserControl.Resources>
        <converters:StatusToAppearanceConverter x:Key="StatusToAppearanceConverter"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Column: Create New Schedule -->
        <Border Grid.Column="0" Background="#1a1c23" CornerRadius="12" Padding="20" BorderBrush="#2d2f39" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel DataContext="{Binding Scheduler}">
                    <TextBlock Text="Lập lịch tác vụ" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

                    <TextBlock Text="Tên tác vụ" Foreground="#9ca3af" FontSize="12" Margin="0,0,0,4"/>
                    <ui:TextBox Text="{Binding NewScheduleName, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Ví dụ: Farm Acc Sáng..."/>

                    <TextBlock Text="Chọn kịch bản" Foreground="#9ca3af" FontSize="12" Margin="0,16,0,4"/>
                    <ComboBox ItemsSource="{Binding AvailableScripts}" SelectedItem="{Binding SelectedScript}" 
                              DisplayMemberPath="Name" Background="#0d0e12" Foreground="White" BorderBrush="#3f424e"/>

                    <TextBlock Text="Thiết bị (Chọn thiết bị ở tab Devices trước)" Foreground="#9ca3af" FontSize="12" Margin="0,16,0,4"/>
                    <Border Background="#0d0e12" CornerRadius="4" BorderBrush="#3f424e" BorderThickness="1" MinHeight="100" MaxHeight="200">
                        <ListBox ItemsSource="{Binding AvailableDevices}" Background="Transparent" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected}" Margin="4">
                                        <TextBlock Text="{Binding DisplayName}" Foreground="White" FontSize="11"/>
                                    </CheckBox>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>

                    <Grid Margin="0,16,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Độ trễ (giây)" Foreground="#9ca3af" FontSize="12" Margin="0,0,0,4"/>
                            <ui:TextBox Text="{Binding DelayBetweenDevices}" PlaceholderText="10"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Lặp lại (lần)" Foreground="#9ca3af" FontSize="12" Margin="0,0,0,4"/>
                            <ui:TextBox Text="{Binding RepeatCount}" PlaceholderText="1"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Thời gian bắt đầu" Foreground="#9ca3af" FontSize="12" Margin="0,16,0,4"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="4"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="4"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <DatePicker Grid.Column="0" SelectedDate="{Binding StartDate}" Background="#0d0e12" BorderBrush="#3f424e"/>
                        
                        <ui:TextBox Grid.Column="2" Text="{Binding StartHour}" PlaceholderText="HH" MaxLength="2"/>
                        <ui:TextBox Grid.Column="4" Text="{Binding StartMinute}" PlaceholderText="MM" MaxLength="2"/>
                        <ui:TextBox Grid.Column="6" Text="{Binding StartSecond}" PlaceholderText="SS" MaxLength="2"/>
                    </Grid>

                    <ui:Button Content="Tạo lịch chạy" Command="{Binding CreateScheduleCommand}" 
                               Appearance="Primary" Margin="0,32,0,0" HorizontalAlignment="Stretch" Height="40" Icon="Add24"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Right Column: Active Schedules -->
        <Grid Grid.Column="1" Margin="24,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Danh sách lịch đã tạo" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

            <ui:DataGrid Grid.Row="1" ItemsSource="{Binding Scheduler.Schedules}" 
                         AutoGenerateColumns="False" Background="#1a1c23" BorderBrush="#2d2f39" 
                         CanUserAddRows="False" SelectionMode="Single">
                <ui:DataGrid.Columns>
                    <DataGridTextColumn Header="Tên tác vụ" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Kịch bản" Binding="{Binding ScriptName}" Width="*"/>
                    <DataGridTextColumn Header="Thiết bị" Binding="{Binding DeviceSerials.Count}" Width="Auto"/>
                    <DataGridTemplateColumn Header="Trạng thái" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ui:Badge Content="{Binding Status}" Padding="8,2" Appearance="{Binding Status, Converter={StaticResource StatusToAppearanceConverter}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Thao tác" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <ui:Button Command="{Binding DataContext.Scheduler.StartScheduleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                               CommandParameter="{Binding}" Appearance="Transparent" Padding="4">
                                        <ui:SymbolIcon Symbol="Play24" Foreground="#34d399"/>
                                    </ui:Button>
                                    <ui:Button Command="{Binding DataContext.Scheduler.DeleteScheduleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                               CommandParameter="{Binding}" Appearance="Transparent" Padding="4">
                                        <ui:SymbolIcon Symbol="Delete24" Foreground="#f87171"/>
                                    </ui:Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </ui:DataGrid.Columns>
            </ui:DataGrid>
        </Grid>
    </Grid>
</UserControl>
