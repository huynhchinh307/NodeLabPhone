<UserControl x:Class="NodeLabFarm.Views.DevicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:NodeLabFarm.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Filter Bar -->
            <RowDefinition Height="*"/>    <!-- DataGrid -->
            <RowDefinition Height="Auto"/> <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Devices" FontSize="32" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
            <ui:Button HorizontalAlignment="Right" Content="Reload" Command="{Binding RefreshCommand}" Appearance="Primary" Padding="16,8">
                <ui:Button.Icon>
                    <ui:SymbolIcon Symbol="ArrowSync24"/>
                </ui:Button.Icon>
            </ui:Button>
        </Grid>

        <!-- Filter Bar -->
        <ui:Card Grid.Row="1" Margin="0,0,0,24" Padding="12" Background="#1a1c23" BorderBrush="#2d2f39">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:SymbolIcon Symbol="Delete24" Foreground="#9ca3af" Margin="0,0,12,0"/>
                    <TextBlock Text="Manage Devices" Foreground="#e5e7eb" VerticalAlignment="Center" FontWeight="SemiBold"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="24,0,0,0">
                    <ui:DropDownButton Content="All" Width="100" Appearance="Secondary" Margin="0,0,12,0"/>
                    <ui:DropDownButton Content="All" Width="100" Appearance="Secondary"/>
                </StackPanel>

                <ui:TextBox Grid.Column="2" PlaceholderText="Search devices..." MaxWidth="400" HorizontalAlignment="Right" Margin="12,0"
                            Text="{Binding DeviceSearchText, UpdateSourceTrigger=PropertyChanged}">
                    <ui:TextBox.Icon>
                        <ui:SymbolIcon Symbol="Search24"/>
                    </ui:TextBox.Icon>
                </ui:TextBox>

                <StackPanel Grid.Column="3" Orientation="Horizontal">
                    <ui:Button Appearance="Secondary" Padding="8" ToolTip="Lọc">
                        <ui:Button.Icon>
                            <ui:SymbolIcon Symbol="Filter24"/>
                        </ui:Button.Icon>
                    </ui:Button>
                    <ui:Button Appearance="Secondary" Padding="8" Margin="8,0,0,0" ToolTip="Chế độ lưới">
                        <ui:Button.Icon>
                            <ui:SymbolIcon Symbol="Grid24"/>
                        </ui:Button.Icon>
                    </ui:Button>
                </StackPanel>
            </Grid>
        </ui:Card>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Devices}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" Background="Transparent" BorderThickness="0"
                  GridLinesVisibility="None" RowHeaderWidth="0" SelectionUnit="FullRow"
                  IsReadOnly="True" SelectionMode="Single"
                  RowHeight="{Binding DeviceRowHeight}" HeadersVisibility="Column"
                  ColumnWidth="*"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <DataGrid.Resources>
                <!-- Existing styles... -->
                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#9ca3af"/>
                    <Setter Property="Height" Value="40"/>
                    <Setter Property="Padding" Value="12,0"/>
                    <Setter Property="FontSize" Value="12"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#2d2f39"/>
                </Style>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#1f2128"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#21232b"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>

                <!-- Device Name & Thumbnail -->
                <DataGridTemplateColumn Header="Device Name" Width="1.5*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Width="40" Height="40" CornerRadius="8" Margin="12,4,12,4" Background="#21232b">
                                    <Image Source="/Assets/devices.png" Stretch="Uniform" Margin="4"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <Grid>
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="White" FontSize="13" TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsRenaming}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <ui:TextBox Text="{Binding CustomName, UpdateSourceTrigger=PropertyChanged}" 
                                                    FontWeight="SemiBold" FontSize="13" 
                                                    Padding="4,2" MinWidth="120">
                                            <ui:TextBox.Style>
                                                <Style TargetType="ui:TextBox" BasedOn="{StaticResource {x:Type ui:TextBox}}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsRenaming}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ui:TextBox.Style>
                                        </ui:TextBox>
                                    </Grid>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Model -->
                <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="*" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#e5e7eb"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="12,0"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- IP Address -->
                <DataGridTextColumn Header="IP Address" Binding="{Binding IpAddress}" Width="*" IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#e5e7eb"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="12,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Status Pill -->
                <DataGridTemplateColumn Header="Status" Width="Auto" MinWidth="155">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                                <Border Padding="6,2" CornerRadius="4" HorizontalAlignment="Left" Margin="12,0" Height="22">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#2d2f39"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Active">
                                                    <Setter Property="Background" Value="#1a3225"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Idle">
                                                    <Setter Property="Background" Value="#312a1a"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Disconnected">
                                                    <Setter Property="Background" Value="#321a1a"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Offline">
                                                    <Setter Property="Background" Value="#321a1a"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="6" Height="6" Margin="0,0,6,0" VerticalAlignment="Center">
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Setter Property="Fill" Value="#9ca3af"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Active">
                                                            <Setter Property="Fill" Value="#4ade80"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Idle">
                                                            <Setter Property="Fill" Value="#fbbf24"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Disconnected">
                                                            <Setter Property="Fill" Value="#f87171"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Offline">
                                                            <Setter Property="Fill" Value="#f87171"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>
                                        <TextBlock Text="{Binding Status}" FontSize="10" FontWeight="SemiBold" VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#9ca3af"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Active">
                                                            <Setter Property="Foreground" Value="#4ade80"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Idle">
                                                            <Setter Property="Foreground" Value="#fbbf24"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Disconnected">
                                                            <Setter Property="Foreground" Value="#f87171"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Offline">
                                                            <Setter Property="Foreground" Value="#f87171"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Actions -->
                <DataGridTemplateColumn Header="Actions" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0">
                                <ui:Button Appearance="Secondary" 
                                           Padding="8" 
                                           Margin="0,0,8,0"
                                           ToolTip="Mở điều khiển thiết bị"
                                           Command="{Binding DataContext.OpenDeviceCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           CommandParameter="{Binding}">
                                    <ui:Button.Icon>
                                        <ui:SymbolIcon Symbol="Desktop24"/>
                                    </ui:Button.Icon>
                                </ui:Button>
                                
                                <ui:Button Appearance="Secondary" 
                                           Padding="8" 
                                           Margin="0,0,8,0"
                                           ToolTip="Đổi tên thiết bị"
                                           Command="{Binding DataContext.RenameDeviceCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           CommandParameter="{Binding}">
                                    <ui:Button.Icon>
                                        <ui:SymbolIcon Symbol="Edit24"/>
                                    </ui:Button.Icon>
                                </ui:Button>

                                <ui:Button Appearance="Secondary" 
                                           Padding="8" 
                                           ToolTip="Làm mới trạng thái thiết bị"
                                           Command="{Binding DataContext.RefreshCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           CommandParameter="{Binding}">
                                    <ui:Button.Icon>
                                        <ui:SymbolIcon Symbol="ArrowSync24"/>
                                    </ui:Button.Icon>
                                </ui:Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <Grid Grid.Row="3" Margin="0,24,0,0">
            <TextBlock Text="{Binding DevicesCountText}" Foreground="#9ca3af" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Appearance="Secondary" Padding="8" Margin="0,0,4,0" ToolTip="Trang trước"
                           Command="{Binding PreviousDevicePageCommand}">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="ChevronLeft24"/>
                    </ui:Button.Icon>
                </ui:Button>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                    <TextBlock Text="Trang" Foreground="#9ca3af" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding DeviceCurrentPage}" FontWeight="Bold" Foreground="White" Margin="8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="/" Foreground="#9ca3af" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding DeviceTotalPages}" Foreground="#9ca3af" Margin="8,0" VerticalAlignment="Center"/>
                </StackPanel>

                <ui:Button Appearance="Secondary" Padding="8" ToolTip="Trang sau"
                           Command="{Binding NextDevicePageCommand}">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="ChevronRight24"/>
                    </ui:Button.Icon>
                </ui:Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
